syntax = "proto3";

// simulating neighboring .proto file
// but different package
package foo;

import "deep/bar/bar.proto";

message Response {
    string response = 1;
    bar.DeepBar deepbar = 2;
}
